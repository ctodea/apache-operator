---
# Use the 'create api' subcommand to add watches to this file.
- version: v1alpha1
  group: web.example.com
  kind: Apache
  role: apache
  # blacklist: # prevent configmap from reconciling
  #   - group: ""
  #     version: v1
  #     kind: ConfigMap

#+kubebuilder:scaffold:watch
# - version: v1
#   group: ""
#   kind: ConfigMap
#   role: apache
#   selector:
#     matchLabels:
#       conf: httpdconf
    